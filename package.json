{
  "name": "reader",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:worker": "npm --prefix worker run dev",
    "build": "npm --prefix frontend run build",
    "deploy": "npm --prefix worker run deploy",
    "install:all": "npm --prefix frontend install && npm --prefix worker install"
  }
}
